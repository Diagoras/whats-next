[project]
name = "whats-next"
version = "0.1.0"
description = "Search your Google Maps saved places by keyword, notes, and travel time"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=25.1.0",
    "shapely>=2.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
